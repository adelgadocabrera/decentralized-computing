use rustchain::blockchain::block::{create_genesis_block, next_block, Block, Transaction};

fn main() {
    // let genesis = create_genesis_block();
    // let nonce: u64 = 0;
    // let transactions: Vec<Transaction> = vec![];
    // let merkle_root: [u8; 32] = [0; 32];
    // let difficulty: u64 = 0;
    // let pow: u64 = 9;
    // let new_block: Block = next_block(&genesis, nonce, transactions, merkle_root, difficulty, pow);

    // println!("{}", genesis);
    // println!("{}", new_block);
}
